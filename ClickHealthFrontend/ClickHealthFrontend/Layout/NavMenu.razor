@using Microsoft.AspNetCore.Components.Authorization
@inject AuthenticationStateProvider AuthStateProvider
@inject NavigationManager Navigation

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">ClickHealth</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable">
    <nav class="flex-column">
        @if (isAuthenticated)
        {
            <!-- Common Home link -->
            <div class="nav-item px-3">
                <NavLink href="/" class="nav-link" Match="NavLinkMatch.All">Home</NavLink>
            </div>

            <!-- Admin Menu -->
            @if (userRole == "Admin")
            {
                <div class="nav-item px-3">
                    <NavLink href="/admin/dashboard" class="nav-link">Dashboard</NavLink>
                    <NavLink href="/admin/approvals" class="nav-link">User Approvals</NavLink>
                </div>
            }

            <!-- Medical Team Menu -->
            else if (userRole == "Medical_Team")
            {
                <div class="nav-item px-3">
                    <NavLink href="/medical/dashboard" class="nav-link">Dashboard</NavLink>
                    <NavLink href="/medical/content-upload" class="nav-link">Upload Content</NavLink>
                    <NavLink href="/medical/content-management" class="nav-link">Content Management</NavLink>
                    <NavLink href="/medical/content-workflow" class="nav-link">Workflow</NavLink>

                </div>
            }


            <!-- Doctor / Patient Menu -->
            else if (userRole == "Doctor" || userRole == "Patient")
            {
                <div class="nav-item px-3">
                    <NavLink href="/user/dashboard" class="nav-link">Dashboard</NavLink>
                    <NavLink href="/user/profile" class="nav-link">Profile</NavLink>
                </div>
            }

            <!-- Logout -->
            <div class="nav-item px-3">
                <a href="#" class="nav-link" @onclick="Logout">Logout</a>
            </div>
        }
        else
        {
            <!-- Unauthenticated User Menu -->
            <div class="nav-item px-3">
                <NavLink href="/login" class="nav-link">Login</NavLink>
                <NavLink href="/register" class="nav-link">Register</NavLink>
                <NavLink href="/admin-login" class="nav-link">Admin Login</NavLink>
            </div>
        }
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;
    private bool isAuthenticated = false;
    private string userRole = "";
    private string userEmail = "";

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    protected override async Task OnInitializedAsync()
    {
        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        var user = authState.User;

        isAuthenticated = user.Identity?.IsAuthenticated ?? false;

        if (isAuthenticated)
        {
            userEmail = user.FindFirst(c => c.Type == "email")?.Value ?? "";
            userRole = user.FindFirst(c => c.Type == "role")?.Value ?? "";
        }
    }

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

    private async Task Logout()
    {
        if (AuthStateProvider is CustomAuthStateProvider authProvider)
        {
            await authProvider.LogoutAsync();
        }

        // Redirect to login after logout
        Navigation.NavigateTo("/login", forceLoad: true);
    }
}
